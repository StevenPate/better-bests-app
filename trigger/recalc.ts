import { logger } from "@trigger.dev/sdk";

/** Placeholder — implemented in Task 3.4. */
export async function recalcWeeks(weekDates: string[]): Promise<void> {
  logger.warn("recalcWeeks not yet implemented", { weekDates });
}
