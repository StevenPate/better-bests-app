# Better Bestsellers environment variables
# Copy this file to `.env` (or `.env.local`) and fill in the required values before running the app.

# Supabase project configuration (required)
VITE_SUPABASE_URL="https://your-project.supabase.co"
VITE_SUPABASE_ANON_KEY="your-public-anon-key"

# Optional: Google Books API key for higher quota / private analytics
# Uncomment and provide a key if you have one configured in Google Cloud.
# VITE_GOOGLE_BOOKS_API_KEY=""

# Optional: Enable verbose debug logging in production
# By default, debug/info logs are suppressed in production builds
# Set to "true" to enable verbose logging for troubleshooting
# VITE_ENABLE_DEBUG_LOGS="true"

# Optional: Environment identifier
# Options: "production", "staging", "development"
# Useful for environment-specific behavior and debugging
# VITE_ENVIRONMENT="development"

# Optional: Feature flags
# VITE_ENABLE_MULTI_REGION="false"  # Enable multi-region functionality (8 regional associations)

# Umami Analytics (Optional)
VITE_UMAMI_WEBSITE_ID=your-website-id-here
VITE_UMAMI_ENABLED=true  # Set to false to disable tracking
VITE_UMAMI_HOST=https://cloud.umami.is
